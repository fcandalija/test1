package com.example;

import java.util.Date;

/**
 * A time machine.
 */
public class TimeMachine {
    
    /**
     * Returns true if {@code date} is in the past for the current time machine date, false
     * else.
     * 
     * @param date
     *            the date to known if it is in the past for the current time machine date.
     * 
     * @return true if {@code date} is in the past for the current time machine date, false
     *         else.
     */
    public boolean isPast(
            final Date date)
    {
    	return false;
    }
    
}
